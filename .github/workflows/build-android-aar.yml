name: Build AndroidLibXrayLite AAR (HEAD + Forced Stable Core)

on:
  workflow_dispatch:

jobs:
  build-aar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout AndroidLibXrayLite (Default Branch)
        uses: actions/checkout@v4
        with:
          repository: 2dust/AndroidLibXrayLite
          submodules: true
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.21"

      - name: Install gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest
          gomobile init

      - name: Install Android SDK + NDK
        run: |
          mkdir -p $HOME/android-sdk/cmdline-tools
          curl -o sdk-tools.zip https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q sdk-tools.zip -d $HOME/android-sdk/cmdline-tools
          mv $HOME/android-sdk/cmdline-tools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest
          
          export ANDROID_HOME=$HOME/android-sdk
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$PATH
          
          yes | sdkmanager --licenses
          sdkmanager "platforms;android-21" "ndk;26.1.10909125" "build-tools;34.0.0"

          echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$HOME/android-sdk/ndk/26.1.10909125" >> $GITHUB_ENV
          echo "$HOME/android-sdk/cmdline-tools/latest/bin" >> $GITHUB_PATH

      - name: Inject TUN Import & Pin Xray
        run: |
          # Force Xray Core to v1.8.4 which definitely has proxy/tun and works
          go mod edit -replace github.com/xtls/xray-core=github.com/xtls/xray-core@v1.8.4
          
          # Create dummy file to force import the TUN module
          echo 'package libv2ray' > force_tun.go
          echo 'import _ "github.com/xtls/xray-core/proxy/tun"' >> force_tun.go
          
          # Tidy up dependencies to ensure the replacement is respected
          go mod tidy

      - name: Build arm64 AAR
        run: |
          gomobile bind -v -target=android/arm64 -androidapi=21 -tags "json,tun" -o libv2ray.aar .
          ls -lh *.aar

      - name: Upload AAR
        uses: actions/upload-artifact@v4
        with:
          name: libv2ray-aar-arm64
          path: "*.aar"
